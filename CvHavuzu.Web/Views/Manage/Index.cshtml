@model IndexViewModel
<p class="text-success">@ViewData["StatusMessage"]</p>
<header class="page-header bg-img size-lg">
    <div class="container">
        <h4 class="text-center">Hesabınızı yönetin</h4>
    </div>
</header>
<main>
    <section class="no-padding-top bg-alt">
        <div class="container">
            <div>
                <hr />
                <dl class="dl-horizontal">
                    <dt>Şifre</dt>
                    <dd>
                        @if (Model.HasPassword)
                        {
                            <a asp-controller="Manage" asp-action="ChangePassword" class="btn-bracketed">Değiştir</a>
                        }
                        else
                        {
                            <a asp-controller="Manage" asp-action="SetPassword" class="btn-bracketed">Oluştur</a>
                        }
                    </dd>
                    <dt>Dışarıdan Kayıt:</dt>
                    <dd>

                        <a asp-controller="Manage" asp-action="ManageLogins" class="btn-bracketed">Yönet</a>
                    </dd>
                    <dt>Telefon:</dt>
                    <dd>
                        <p>
                            Telefon numaraları ikinci bir doğrulama yöntemi olarak kullanılabilir.
                            ASP.NET uygulamanıza  Sms kullanarak iki faktörlü kimlik doğrulamayı destekletmek için
                            <a href="https://go.microsoft.com/fwlink/?LinkID=532713">buraya</a> tıklayınız.
                        </p>
                        @*@(Model.PhoneNumber ?? "None")
                            @if (Model.PhoneNumber != null)
                            {
                                <br />
                                <a asp-controller="Manage" asp-action="AddPhoneNumber" class="btn-bracketed">Change</a>
                                <form asp-controller="Manage" asp-action="RemovePhoneNumber" method="post">
                                    [<button type="submit" class="btn-link">Remove</button>]
                                </form>
                            }
                            else
                            {
                                <a asp-controller="Manage" asp-action="AddPhoneNumber" class="btn-bracketed">Add</a>
                            }*@
                    </dd>

                    <dt>Kimlik Doğrulama:</dt>
                    <dd>
                        <p>
                            Sisteminizde yapılandırılmış iki faktörlü kimlik doğrulama sistemi bulunmamaktadır.İki faktörlü kimlik doğrulamayı uygulamanızda desteklemek için <a href="https://go.microsoft.com/fwlink/?LinkID=532713">buraya</a> tıklayınız.
                        </p>
                        @*@if (Model.TwoFactor)
                            {
                                <form asp-controller="Manage" asp-action="DisableTwoFactorAuthentication" method="post" class="form-horizontal">
                                    Enabled <button type="submit" class="btn-link btn-bracketed">Disable</button>
                                </form>
                            }
                            else
                            {
                                <form asp-controller="Manage" asp-action="EnableTwoFactorAuthentication" method="post" class="form-horizontal">
                                    <button type="submit" class="btn-link btn-bracketed">Enable</button> Disabled
                                </form>
                            }*@
                    </dd>
                </dl>
            </div>
        </div>
    </section>
</main>